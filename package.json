{
  "name": "terrain-climb-racing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "server": "cd server && node index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
